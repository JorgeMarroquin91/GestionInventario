@model IEnumerable<GestionInventario.Models.Kardex>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.saldo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Compras.idCompra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ventas.idVenta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.saldo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Compras.idCompra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ventas.idVenta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idKardex }) |
            @Html.ActionLink("Details", "Details", new { id=item.idKardex }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idKardex })
        </td>
    </tr>
}

</table>
